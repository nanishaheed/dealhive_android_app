package com.simats.dealhive.ui.theme

import androidx.compose.ui.graphics.Color

// Primary colors
val Primary = Color(0xFF3B82F6)  // Vibrant Blue
val Secondary = Color(0xFF64748B)  // Muted Blue/Gray

// Semantic colors
val Success = Color(0xFF22C55E)
val Warning = Color(0xFFEAB308)
val Error = Color(0xFFEF4444)

// Background colors
val Background = Color(0xFF111111)  // Deep Black/Gray
val Surface = Color(0xFF18181B)  // Zinc 900
val SurfaceLight = Color(0xFF27272A)  // Zinc 800

// Text colors
val TextPrimary = Color.White
val TextMuted = Color(0xFFA1A1AA)  // Zinc 400
val TextSecondary = Color(0xFFD4D4D8)  // Zinc 300

// Border
val Border = Color(0xFF27272A)

// Brand colors
val Amazon = Color(0xFFFF9900)
val Flipkart = Color(0xFF2874F0)

// Basic colors
val White = Color.White
val Black = Color.Black
val Transparent = Color.Transparent
